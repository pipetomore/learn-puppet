[main]
  user     =  puppet
  group    =  puppet
  report   =  true
  reports  =  log,store
  
[master]
  certname = <%= @puppet::params::puppetserver %>
  
[agent]
  pluginsync = false
  report     = true
  server     = <%= @puppetserver %>
  